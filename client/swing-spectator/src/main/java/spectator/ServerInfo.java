package spectator;

public class ServerInfo {
  public final String host;
  public final int port;

  public ServerInfo(String serverHost, int serverPort) {
    host = serverHost;
    port = serverPort;
  }
}
